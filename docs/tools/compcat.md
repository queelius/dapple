# compcat -- Renderer Comparison

Compare how an image looks across different renderers side by side.

## Installation

```bash
pip install dapple[compcat]
```

## Usage

```bash
compcat photo.jpg braille sextants quadrants
compcat photo.jpg braille sextants quadrants ascii -w 120
```

Each renderer's output is shown in a titled frame, arranged in a grid.
